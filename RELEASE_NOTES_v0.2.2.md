# GDUT 成绩提醒 v0.2.2

这是一个安装体验和打包修复版本。

## 修复内容

- 安装界面选择不存在但合法的路径时，安装程序会自动创建该文件夹。
- 安装界面会提前检查路径格式，拦截空路径、非法字符、相对路径和不存在的盘符。
- 修复打包 exe 启动时报 `ModuleNotFoundError: No module named 'win32timezone'` 的问题。

## 已验证

- 单元/集成测试通过。
- Python 编译检查通过。
- 安装包重新构建通过。
- 静默安装到新目录、启动、卸载 smoke test 通过。
